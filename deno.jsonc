{
  "compilerOptions": {
    "strict": true
  },
  "tasks": {
    "compile": "deno compile -A -r --config deno.jsonc --output build/server mod.ts",
    "run": "deno run --allow-net --allow-env --allow-write --allow-read --watch mod.ts"
  }
}
